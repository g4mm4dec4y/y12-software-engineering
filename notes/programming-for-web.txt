S.E. Procedure/Module

Tags, attributes > selectors/classes, attributes > functionality

Site controlled through the Document Object Model (Object Oriented)
Class is common in JS and CSS
Selector: properties; attributes and methods

Procedures are part of procedural/imperative/module paradigm
One returns, one makes something happen: functions and subroutines
Function always returns

<input type=“hidden” id=“clicked” value=“no”>
above: stores a value

.innerHTML accesses space within the tags to modify or set.

Dot notation = object.property
attribute vs methods - in js method always has brackets in it
e.g. document.method().attribute 

Line PWA and site is blurry. PWA vs site diff -> functionality.
Uses resources of browser

Local, PWA, server is computer and thus can run offline.
However traditional website has external servers so can't run online

Ring topology - full form is 'token ring'.
Goes around the circle and finds which device matches the token that is carried.

Router - works with switch
Switch - data sent along predetermined paths, most safe/accurate etc, directs packets
Router - basically a GPS for the packets
Was predecadet on data bein open

Implement privacy by design in the code. 

6-word summary
- 3-6 word per paragraph, must come from paragraph basically a summary, can be less, helps remember data
